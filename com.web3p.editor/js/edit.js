/**
 * 
 */

CodeMirror(document.getElementById('code'), {
    lineNumbers: true,
    tabSize: 4,
    value: 'console.log("Hello, World");',
    mode: 'javascript',
    theme: 'cobalt'
  });
  
  function loaded() {}